# AR 双手手势交互特效 — 任务分工

> 两人按「识别/数据管线」与「渲染/滤镜视觉」两条线分工。
> **A 负责把「手和人在哪儿」算出来；B 负责把「三块滤镜长什么样」画出来。**
> 开工第一步先一起定死数据接口，之后并行开发、互不阻塞。

---

## 任务分工表

| 编号 | 任务 | 负责人 | 产出文件 | 备注 |
|---|---|---|---|---|
| 0 | 定数据接口 `FrameData` + 搭骨架 + 本地服务跑通 | A+B | 骨架文件 | 开工第一步，双方按接口开发 |
| A1 | 摄像头 `getUserMedia` + 权限/无摄像头/被占用错误处理 | A | app.js（管线部分） | 三类错误均给中文提示 |
| A2 | 画面镜像、cover 裁切、窗口缩放、坐标映射 | A | app.js | 归一化↔显示坐标互转 |
| A3 | MediaPipe Hands 加载（`maxNumHands:2`）+ 状态提示 | A | app.js | 加载中提示、失败报错 |
| A4 | 左右手按画面 x 排序（不用 handedness 标签） | A | app.js | 镜像后左右易反，重点 |
| A5 | 21 点插值平滑（接「跟手平滑度」滑块） | A | app.js | 平衡抖动与延迟 |
| A6 | Selfie Segmentation 人物遮罩输出 | A | app.js | 输出 mask 给 B |
| A7 | 主循环 + 性能控制 | A | app.js | `requestAnimationFrame` |
| A8 | 演示模式假手数据源 | A | app.js | **最早做**，B 不等摄像头 |
| A9 | `Start-LocalServer.ps1`（自动选空闲端口） | A | Start-LocalServer.ps1 | PowerShell |
| A10 | `启动AR.bat`（双击调 ps1 + 开浏览器） | A | 启动AR.bat | 禁止 file:// |
| B1 | `index.html` 结构 + `styles.css`（全屏/深色/中文） | B | index.html, styles.css | 启动后弱化 UI |
| B2 | Canvas 合成管线（video+遮罩+三面片分层） | B | app.js（渲染部分） | 合成顺序 |
| B3 | 三面片四边形几何 + 实时变形 | B | app.js | 处理凹/不共面 |
| B4 | 红色半调滤镜（网点/颗粒/印刷） | B | app.js | 亮度分档 |
| B5 | 蓝色冷调滤镜（双色调/热成像/旧电视） | B | app.js | 人物细节清晰 |
| B6 | 绿色伪彩滤镜（残影/柔边/纸张颗粒） | B | app.js | 非纯绿叠加 |
| B7 | 遮罩羽化 + 模糊 | B | app.js | 消除硬边/闪烁 |
| B8 | 控制按钮交互 + 演示模式视觉 | B | app.js | 4 个按钮 |
| B9 | `使用说明.txt`（UTF-8 中文） | B | 使用说明.txt | 防乱码 |
| C1 | 合并成单文件 `app.js` | A+B | app.js | 交付前合入 |
| C2 | 最终验收（localhost/加载/演示三面片/按钮/无报错） | A+B | — | 逐条过验收清单 |

---

## 依赖关系

- **B 依赖 A8**：B 从第一天就要用 A 的演示假数据开发滤镜，所以 A8 必须最先完成。
- **B 依赖 A6**：遮罩羽化（B7）要等 A 的 mask 出来才能联调，但 B 可先用临时硬编码 mask 占位。
- **A5 ↔ B8**：平滑度滑块是 A、B 的接口点，联调时重点调。

---

## 关键：先一起定死数据接口

这是唯一会互相卡死的地方，开工第一小时就写下来，之后双方照此实现：

```js
// —— 双方约定：A 每帧产出，B 每帧消费 ——
FrameData = {
  // 左右手已按画面位置排序好，x/y 为 0~1 归一化（相对显示画布）
  hands: {
    left:  null | { pts: [{x, y} × 21] },
    right: null | { pts: [{x, y} × 21] },
  },
  mask: Uint8Array | null,   // 255=人物, 0=背景
  w: canvasWidth, h: canvasHeight,
};

// 指尖索引（双方共用常量）：拇指4、食指8、中指12、小指20
// B 直接取 hands.left.pts[4] 等，不必关心点是怎么来的
// A 暴露控制函数，B 只管调用：startCamera / startDemo / reset / setSmooth
```

---

## 避免抢 `app.js` 的方案

交付要求是单文件 `app.js`，但开发时建议：

- **开发期拆成多个 JS**（如 `tracking.js` / `filters.js` / `main.js`），`index.html` 里加多个 `<script>` 标签；**最后再合并成一个 `app.js`** 交付。
- 或用 Git 分两条分支（当前目录还不是 git 仓库，建议先 `git init`），A 和 B 各管自己的文件，定期合并。

---

## 建议节奏（约 2~3 天）

| 阶段 | 做什么 |
|---|---|
| 第 1 步（共同） | 定接口 + 搭骨架：跑通本地服务、空 canvas、把上面 `FrameData` 写死 |
| 第 1 天（并行） | A：摄像头 + MediaPipe 出原始点；B：用 A 给的演示假数据画三块**纯色**四边形跟手 |
| 第 2 天（并行） | A：排序 + 平滑 + 遮罩；B：三个滤镜逐个做 + 遮罩羽化 |
| 第 3 天（合入） | 合并成 `app.js`，联调坐标/遮罩 bug，补齐错误提示和工具链，过验收清单 |

---

## 提前约定的技术决策（避免返工）

1. **性能**：三块滤镜若每帧全画布 `getImageData` 会卡。建议**只在面片包围盒内做像素处理**，或降采样处理再放大。
2. **坐标系**：镜像后「画面左」=「用户右」，左右排序要和显示一致，这是最容易出 bug 的点。
3. **平滑 vs 延迟**：平滑度过高会「跟手慢」，过低会抖，B 的滑块要接到 A 的 lerp 系数上，联调时重点调这个。
