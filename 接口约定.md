# 接口约定（A 实现 / B 消费）

> 这是双方开发的唯一契约。A 只负责实现它，B 只依赖它；任何一方改接口必须先同步本文档。
> 坐标、镜像、裁切、排序、平滑全部由 A 处理，B 拿到的都是「显示坐标系」的最终结果，不做二次换算。

---

## 0. 总体结构

- A 实现全局对象 `window.AR`（识别与数据管线）。
- A 每帧生产 `FrameData`，通过回调交给 B；B 只负责渲染，不碰摄像头 / MediaPipe / 坐标换算。
- UI（B 负责）也只通过 `AR` 控制与监听。

---

## 1. 生命周期与控制方法（B / UI 调用）

```js
AR.init({ canvas }) -> Promise<void>   // 初始化，传入 B 的 <canvas>，不启动摄像头
AR.startCamera()    -> Promise<void>   // 请求摄像头 + 加载 MediaPipe 模型
AR.startDemo()      -> void            // 演示模式：无摄像头，合成假手
AR.reset()          -> void            // 回到初始空闲状态
AR.setSmooth(v)     -> void            // 跟手平滑度 v ∈ [0,1]，0=最跟手，1=最平滑（默认 0.6）
```

---

## 2. 回调与事件（A 通知 B / UI）

```js
// 每帧回调：B 在此渲染
AR.setFrameCallback(cb)   // cb(frame: FrameData) -> void

// 状态事件
AR.on('status',      ({ text }) => {})     // 模型加载等状态提示
AR.on('error',       ({ code, message }) => {})
AR.on('handCount',   ({ count }) => {})    // 0 / 1 / 2，用于「只识别到一只手」提示
AR.on('stateChange', ({ state }) => {})    // idle | loading | camera | demo | error，用于弱化 UI
```

错误码（`error` 事件的 `code`）：

```js
'camera-permission-denied'  // 摄像头权限被拒绝
'camera-not-found'          // 没有找到摄像头
'camera-in-use'             // 摄像头被其他软件占用
'model-load-failed'         // MediaPipe 模型加载失败
```

---

## 3. 每帧数据 FrameData（核心契约）

```js
FrameData = {
  t: number,                  // 时间戳 ms
  w: number,                  // 显示画布宽（CSS px）
  h: number,                  // 显示画布高（CSS px）
  video: CanvasSource | null, // 已镜像+cover裁切+缩放的视频帧；demo 模式为 null
  mask: CanvasSource | null,   // 人物遮罩：离屏画布，尺寸 w×h，alpha=人物置信度0~255；demo 为 null
  hands: {
    left:  HandData | null,   // 画面左侧的手
    right: HandData | null,   // 画面右侧的手
  },
}
```

---

## 4. HandData 与关键点索引

```js
HandData = {
  pts: { x, y }[21],   // 21 点，已平滑；x/y 归一化 0~1（相对显示画布）
  score: number,       // 置信度 0~1
}
```

关键点索引常量（双方共用，写死不改）：

```js
LM = {
  WRIST: 0, THUMB_TIP: 4, INDEX_TIP: 8, MIDDLE_TIP: 12, RING_TIP: 16, PINKY_TIP: 20
}
```

---

## 5. 三面片顶点约定（B 据此建四边形）

```js
QUADS = {
  red:   { a: LM.THUMB_TIP,  b: LM.INDEX_TIP  },   // 拇指4 + 食指8
  blue:  { a: LM.INDEX_TIP,  b: LM.MIDDLE_TIP },   // 食指8 + 中指12
  green: { a: LM.MIDDLE_TIP, b: LM.PINKY_TIP  },   // 中指12 + 小指20
}

// 每块面片 4 个顶点的画法顺序（避免自交/蝴蝶结）：
// [ left.pts[a], left.pts[b], right.pts[b], right.pts[a] ]
```

---

## 6. 坐标系约定（最重要，避免镜像 bug）

- 原点左上，x 向右，y 向下。
- **镜像已由 A 处理**：`pts` 与 `mask` 都是「用户看到的画面」里的位置；`pts[].x = 0` 是画面左边缘、`= 1` 是右边缘。
- `mask` 与 `video` 尺寸均等于 `w × h`，已对齐，可逐像素一一对应。
- B 直接用 `drawImage(frame.video, 0, 0, w, h)`、`fillRect(pts.x * w, pts.y * h)`，无需再换算或翻转。

---

## 7. 演示模式约定

- `startDemo()` 后 A 仍以同样频率调用 `setFrameCallback`，`FrameData` 结构不变。
- `video` 与 `mask` 可为 null（B 自画深色背景）；`hands` 为两只带轻微运动的合成假手。
- 用途：B 无摄像头时开发/测试滤镜与三面片，也用于验收「演示模式显示三块面片」。

---

## 8. 已定的默认决策（如需改，现在提）

1. 渲染清晰度按 CSS px（不启用 DPR）；若需更清晰，A 内部按 DPR 渲染，`w/h` 相应变为设备像素，但契约不变（`w/h` 与 `video/mask` 尺寸恒一致）。
2. 演示模式 `mask` / `video` 为 null，B 自画背景。
3. `setSmooth` 默认值 0.6；`k`（低通系数）由 A 内部映射，不在契约内。

---

## 附：最小使用示例（B 侧渲染骨架）

```js
AR.setFrameCallback((f) => {
  ctx.clearRect(0, 0, f.w, f.h);
  if (f.video) ctx.drawImage(f.video, 0, 0, f.w, f.h);
  else { /* 深色背景 */ }

  if (!f.hands.left || !f.hands.right) return;   // 任一手缺失 → 本帧不画面片

  // 红：connect(f.hands.left, f.hands.right, QUADS.red)   ...
  // 蓝：connect(f.hands.left, f.hands.right, QUADS.blue)  ...
  // 绿：connect(f.hands.left, f.hands.right, QUADS.green) ...
});
```
